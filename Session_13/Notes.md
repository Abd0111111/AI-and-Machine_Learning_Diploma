# Customer Database Management in SQL 🔹

هذا الملف يشرح **إدارة قاعدة بيانات العملاء والمنتجات والطلبات** في SQL خطوة بخطوة.
الشرح بالعربي بأسلوب تعليمي، مع أمثلة عملية لكل جزء، واستخدام الإيموجي لتسهيل الفهم.

---

## 1️⃣ Defining Tables and Relationships 🔥

### تعريف الجداول والعلاقات بينهما

**الشرح:**

* أي قاعدة بيانات تعتمد على **جداول (Tables)** لتخزين البيانات.
* كل جدول يحتوي على **حقول (Columns)** لتخزين نوع معين من المعلومات.
* يجب تحديد **Primary Key (مفتاح أساسي)** لكل جدول لضمان تفرد كل سجل.
* نستخدم **Foreign Key (مفتاح خارجي)** لربط الجداول ببعضها والحفاظ على سلامة البيانات (Data Integrity).
* مثال: جدول `Orders` مرتبط بجدول `Customers` عن طريق CustomerID.

### الجداول الأساسية:

1. **Customers** – تخزن معلومات العملاء (الاسم، العمر، النوع، المنطقة).
2. **Products** – تخزن معلومات المنتجات (اسم المنتج، الفئة، السعر).
3. **Orders** – تخزن معلومات الطلبات (العميل، تاريخ الطلب، المبلغ الكلي).
4. **OrderDetails** – تخزن تفاصيل كل طلب (أي منتج تم شراؤه، الكمية).

```sql
-- Customers Table 🔹 جدول العملاء
CREATE TABLE Customers (
    CustomerID INT PRIMARY KEY,        -- معرف العميل (مفتاح أساسي)
    Name NVARCHAR(100),                -- اسم العميل
    Age INT,                            -- العمر
    Gender NVARCHAR(10),               -- النوع
    Region NVARCHAR(50)                -- المنطقة
);

-- Products Table 🔹 جدول المنتجات
CREATE TABLE Products (
    ProductID INT PRIMARY KEY,          -- معرف المنتج
    ProductName NVARCHAR(100),          -- اسم المنتج
    Category NVARCHAR(50),              -- التصنيف
    Price DECIMAL(10, 2)                -- السعر
);

-- Orders Table 🔹 جدول الطلبات
CREATE TABLE Orders (
    OrderID INT PRIMARY KEY,            -- معرف الطلب
    CustomerID INT,                     -- معرف العميل
    OrderDate DATE,                     -- تاريخ الطلب
    TotalAmount DECIMAL(10, 2),         -- إجمالي السعر
    FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID)  -- علاقة بالعملاء
);

-- OrderDetails Table 🔹 تفاصيل الطلبات
CREATE TABLE OrderDetails (
    OrderDetailID INT PRIMARY KEY,       -- معرف تفاصيل الطلب
    OrderID INT,                         -- معرف الطلب
    ProductID INT,                       -- معرف المنتج
    Quantity INT,                        -- الكمية
    FOREIGN KEY (OrderID) REFERENCES Orders(OrderID),          -- علاقة بالطلبات
    FOREIGN KEY (ProductID) REFERENCES Products(ProductID)     -- علاقة بالمنتجات
);
```

**ملاحظة:**

* العلاقة بين `Orders` و `Customers` هي **1 إلى متعدد (1:N)** → كل عميل يمكن أن يكون له عدة طلبات.
* العلاقة بين `Orders` و `OrderDetails` هي **1 إلى متعدد (1:N)** → كل طلب يمكن أن يحتوي على منتجات متعددة.

---

## 2️⃣ Populating Tables with Data 🔥

### إدخال بيانات تجريبية لكل جدول

#### 2.1 Customers 🔹 العملاء

```sql
INSERT INTO Customers (CustomerID, Name, Age, Gender, Region)
VALUES  
(1, N'Ahmed Hassan', 30, N'Male', N'Cairo'),
(2, N'Mohamed Tarek', 42, N'Male', N'Alexandria'),
(3, N'Heba Ali', 28, N'Female', N'Giza'),
(4, N'Salma Nabil', 35, N'Female', N'Mansoura'),
(5, N'Youssef Adel', 39, N'Male', N'Cairo'),
(6, N'Nourhan Samir', 25, N'Female', N'Ismailia');
```

#### 2.2 Products 🔹 المنتجات

```sql
INSERT INTO Products (ProductID, ProductName, Category, Price)
VALUES
(1, N'Laptop Lenovo', N'Electronics', 15000.00),
(2, N'Smartphone Samsung', N'Electronics', 10000.00),
(3, N'Bluetooth Speaker', N'Electronics', 1200.00),
(4, N'Office Chair', N'Furniture', 1800.00),
(5, N'Water Bottle', N'Kitchen', 80.00),
(6, N'Notebook A5', N'Stationery', 25.00),
(7, N'Pen Pack', N'Stationery', 40.00),
(8, N'Monitor HP', N'Electronics', 4000.00);
```

#### 2.3 Orders 🔹 الطلبات

```sql
INSERT INTO Orders (OrderID, CustomerID, OrderDate, TotalAmount)
VALUES
(1, 1, '2024-12-01', 15225.00),
(2, 2, '2025-01-15', 10125.00),
(3, 3, '2025-01-20', 1225.00);
```

#### 2.4 OrderDetails 🔹 تفاصيل الطلبات

```sql
INSERT INTO OrderDetails (OrderDetailID, OrderID, ProductID, Quantity)
VALUES
(1, 1, 1, 1),   -- Laptop Lenovo
(2, 1, 6, 3);   -- Notebook
```

---

## 3️⃣ Basic Queries 🔥

### استعلام البيانات الأساسية

**الشرح التفصيلي:**

* **Queries** هي الطريقة الأساسية لاستخراج البيانات من قاعدة البيانات.
* هناك أنواع مختلفة من الاستعلامات:

  * استعلامات بسيطة SELECT
  * استعلامات تجميعية باستخدام Aggregate Functions
  * استعلامات مرتبطة بجداول أخرى باستخدام JOIN

#### 3.1 عرض أول 10 عملاء

**الشرح:**

* نستخدم `SELECT TOP 10 *` للحصول على أول 10 سجلات من جدول العملاء.
* مفيد عند مراجعة جزء من البيانات بسرعة دون تحميل كل الجدول.

```sql
SELECT TOP 10 * FROM Customers;  -- عرض أول 10 عملاء
```

**مثال عملي:**

* يعرض أول 10 سجلات في جدول Customers مع كل الأعمدة: CustomerID, Name, Age, Gender, Region.

---

#### 3.2 إجمالي عدد العملاء

**الشرح:**

* نستخدم `COUNT(*)` لحساب عدد الصفوف في جدول العملاء.
* `AS TotalCustomers` يعطي اسمًا واضحًا للعمود الناتج.
* مفيد لمعرفة حجم قاعدة البيانات أو مراقبة عدد العملاء.

```sql
SELECT COUNT(*) AS TotalCustomers FROM Customers;  -- عدد العملاء الإجمالي
```

**مثال عملي:**

* إذا كان لدينا 6 سجلات، النتيجة ستكون:

  * TotalCustomers: 6

---

#### 3.3 متوسط عمر العملاء

**الشرح:**

* نستخدم `AVG(Age)` لحساب متوسط الأعمار لجميع العملاء.
* يساعد في فهم التركيبة العمرية للعملاء.

```sql
SELECT AVG(Age) AS AverageCustomerAge FROM Customers;  -- متوسط عمر العملاء
```

**مثال عملي:**

* إذا كانت الأعمار: 30, 42, 28, 35, 39, 25
* المتوسط = (30+42+28+35+39+25)/6 = 33.16 تقريبًا
* النتيجة: AverageCustomerAge: 33.16

---

💡 **Takeaways:**

* استعلامات SQL الأساسية ضرورية للوصول السريع للبيانات.
* `SELECT TOP N` → استعراض عدد محدد من الصفوف.
* `COUNT()` → عد الصفوف.
* `AVG()` → حساب المتوسط.
* هذه الاستعلامات مفيدة لفهم البيانات الأولية قبل تطبيق التحليلات الأكثر تعقيدًا.


## 4️⃣ Aggregate Functions & GROUP BY 🔥

### فهم الدوال التجميعية وGROUP BY

**الشرح التفصيلي:**

* `GROUP BY` تُستخدم لتجميع الصفوف في الجدول بناءً على عمود أو أكثر له قيم مشتركة.
* كل مجموعة يمكن تطبيق **الدوال التجميعية (Aggregate Functions)** عليها لحساب إحصاءات مثل:

  * `COUNT()` → عدد الصفوف في كل مجموعة
  * `SUM()` → مجموع القيم في كل مجموعة
  * `AVG()` → متوسط القيم في كل مجموعة
  * `MAX()` → أكبر قيمة في كل مجموعة
  * `MIN()` → أصغر قيمة في كل مجموعة

💡 **متى نستخدم GROUP BY؟**

* عندما نريد **تحليل البيانات بحسب تصنيف معين** مثل: العملاء حسب المنطقة، المنتجات حسب الفئة، أو الإيرادات لكل عميل.
* بدون `GROUP BY`، لا يمكن حساب إحصاءات لكل مجموعة من البيانات بشكل منفصل.

---

#### 4.1 عدد العملاء لكل منطقة

**الشرح:**

* نريد معرفة عدد العملاء في كل منطقة.
* نستخدم `GROUP BY Region` لتجميع العملاء حسب منطقتهم.
* `COUNT(*)` تحسب عدد العملاء في كل مجموعة.

```sql
SELECT Region, COUNT(*) AS CustomersPerRegion
FROM Customers
GROUP BY Region;
```

**مثال عملي:**

* القاهرة: 2 عميل
* الجيزة: 1 عميل
* الإسكندرية: 1 عميل
* Mansoura: 1 عميل
* Ismailia: 1 عميل

---

#### 4.2 إجمالي الإنفاق لكل عميل

**الشرح:**

* نريد حساب مجموع إنفاق كل عميل على كل الطلبات.
* نربط جدول `Customers` مع جدول `Orders` عبر CustomerID.
* نستخدم `SUM(O.TotalAmount)` لجمع قيمة الطلبات لكل عميل.
* `GROUP BY C.Name` لتجميع البيانات حسب اسم العميل.

```sql
SELECT C.Name, SUM(O.TotalAmount) AS TotalSpent
FROM Customers C
JOIN Orders O ON C.CustomerID = O.CustomerID
GROUP BY C.Name;
```

**متى نستخدمه؟**

* لمعرفة أكثر العملاء إنفاقًا.
* لمقارنة العملاء بحسب المبلغ الإجمالي الذي أنفقوه.

**مثال عملي:**

* Ahmed Hassan: 15345.00
* Mohamed Tarek: 10125.00
* Heba Ali: 1225.00

---

#### 4.3 الإيرادات حسب تصنيف المنتجات

**الشرح:**

* نريد معرفة الإيرادات لكل فئة من المنتجات (Electronics, Furniture, Kitchen, Stationery).
* نربط جدول `OrderDetails` مع `Products` للحصول على سعر المنتج والفئة.
* نحسب الإيراد لكل منتج باستخدام `OD.Quantity * P.Price` ثم نجمعها لكل فئة باستخدام `SUM()`.
* `GROUP BY P.Category` لتجميع البيانات حسب الفئة.

```sql
SELECT P.Category, SUM(OD.Quantity * P.Price) AS RevenuePerCategory
FROM OrderDetails OD
JOIN Products P ON OD.ProductID = P.ProductID
GROUP BY P.Category;
```

**متى نستخدمه؟**

* لتحليل الأداء المالي لكل فئة منتجات.
* لتحديد أي فئة تحقق أعلى مبيعات.

**مثال عملي:**

* Electronics: 35225.00
* Furniture: 1800.00
* Kitchen: 160.00
* Stationery: 220.00

---

💡 **Takeaways:**

* `GROUP BY` + Aggregate Functions هي الطريقة الأساسية لتحليل البيانات الإحصائية في SQL.
* تمكننا من الإجابة على أسئلة مثل: من أكثر العملاء شراءً؟ أي الفئات تحقق أعلى مبيعات؟ كم عدد العملاء في كل منطقة؟
* دائمًا يجب أن نستخدم `GROUP BY` مع الأعمدة غير التجميعية التي نريد عرضها.

## 5️⃣ Joins in SQL 🔥

### أنواع الربط بين الجداول (Joins)

**الشرح التفصيلي:**

* في قواعد البيانات، البيانات عادةً مقسمة على جداول متعددة.
* **Joins** تسمح لنا بدمج هذه الجداول للحصول على معلومات متكاملة.
* كل نوع Join له وظيفة محددة ويحدد أي الصفوف تظهر وأيها لا تظهر.

#### 5.1 INNER JOIN 🔹 ربط داخلي

* يعرض **الصفوف التي لها تطابق في كل الجداول المشاركة فقط**.
* أي صف لا يوجد له مقابل في الجدول الآخر **لن يظهر**.

**مثال عملي: عرض جميع الطلبات مع اسم العميل والمنتج والكمية والسعر لكل خط طلب**

```sql
SELECT 
    O.OrderID,                    -- رقم الطلب
    C.Name AS CustomerName,       -- اسم العميل
    P.ProductName,                -- اسم المنتج
    OD.Quantity,                  -- الكمية
    (OD.Quantity * P.Price) AS TotalLinePrice  -- السعر الإجمالي للمنتج في الطلب
FROM Orders O
INNER JOIN Customers C ON O.CustomerID = C.CustomerID  -- ربط الطلبات بالعملاء
INNER JOIN OrderDetails OD ON O.OrderID = OD.OrderID    -- ربط تفاصيل الطلبات
INNER JOIN Products P ON OD.ProductID = P.ProductID;    -- ربط المنتجات
```

**شرح:**

* فقط الطلبات التي تحتوي على **عميل حقيقي + تفاصيل الطلب + منتج موجود** تظهر.
* مثال: طلب بدون منتجات لن يظهر.

---

#### 5.2 LEFT JOIN 🔹 ربط أيسر

* يعرض **كل الصفوف من الجدول الأيسر** حتى لو لم يوجد لها تطابق في الجدول الأيمن.
* الصفوف غير المطابقة تظهر بقيم `NULL`.

**مثال عملي: عرض كل الطلبات، حتى لو لم تحتوي على منتجات بعد**

```sql
SELECT 
    O.OrderID,
    C.Name AS CustomerName,
    P.ProductName,
    OD.Quantity
FROM Orders O
LEFT JOIN Customers C ON O.CustomerID = C.CustomerID
LEFT JOIN OrderDetails OD ON O.OrderID = OD.OrderID
LEFT JOIN Products P ON OD.ProductID = P.ProductID;
```

**شرح:**

* نضمن أن **كل الطلبات تظهر** حتى لو لم يتم إضافة منتجات لها بعد.
* الصفوف التي لم يتم إدخال تفاصيل لها ستكون `NULL` في عمود المنتج أو الكمية.

---

#### 5.3 RIGHT JOIN 🔹 ربط أيمن

* يعرض **كل الصفوف من الجدول الأيمن** حتى لو لم يوجد لها تطابق في الجدول الأيسر.
* الصفوف غير المطابقة تظهر بقيم `NULL`.

**مثال عملي: عرض كل المنتجات، حتى لو لم يتم طلبها بعد**

```sql
SELECT 
    P.ProductName, 
    P.Price, 
    OD.Quantity, 
    O.OrderDate
FROM OrderDetails OD
RIGHT JOIN Products P ON OD.ProductID = P.ProductID
LEFT JOIN Orders O ON OD.OrderID = O.OrderID;
```

**شرح:**

* كل المنتجات تظهر، حتى المنتجات الجديدة التي لم يتم بيعها بعد.
* الصفوف بدون طلب ستظهر `NULL` في الكمية والتاريخ.

---

#### 5.4 FULL OUTER JOIN 🔹 ربط كامل

* يجمع بين LEFT و RIGHT JOIN.
* يعرض **كل الصفوف من كلا الجدولين**.
* القيم غير المطابقة تظهر `NULL`.

**مثال عملي: عرض كل العملاء وكل الطلبات**

```sql
SELECT 
    C.Name AS CustomerName, 
    O.OrderID, 
    O.OrderDate
FROM Customers C
FULL OUTER JOIN Orders O ON C.CustomerID = O.CustomerID;
```

**شرح:**

* يظهر جميع العملاء، حتى الذين لم يقدموا أي طلب.
* يظهر جميع الطلبات، حتى إذا لم يكن مرتبطًا بأي عميل.
* مثال عملي: العملاء الجدد بدون طلبات أو الطلبات بدون عميل.

---

#### 5.5 INNER JOIN مع فلترة

* يمكن دمج Join مع **WHERE** لتصفية النتائج.
* مثال: عرض جميع الطلبات الخاصة بـ Ahmed Hassan فقط، مرتبة حسب التاريخ.

```sql
SELECT 
    O.OrderID, 
    O.OrderDate, 
    P.ProductName, 
    OD.Quantity
FROM Orders O
INNER JOIN Customers C ON O.CustomerID = C.CustomerID
INNER JOIN OrderDetails OD ON O.OrderID = OD.OrderID
INNER JOIN Products P ON OD.ProductID = P.ProductID
WHERE C.Name = 'Ahmed Hassan'
ORDER BY O.OrderDate DESC;
```

**شرح:**

* نربط جميع الجداول باستخدام INNER JOIN.
* نضيف شرط WHERE لتحديد العميل المحدد.
* نرتب النتائج من الأحدث للأقدم.
* مثال عملي: جميع المنتجات التي اشتراها Ahmed Hassan مع الكمية والسعر.

---

### 💡 نصائح لتذكر Joins بسهولة

* **INNER JOIN** → 👌 فقط الصفوف المطابقة.
* **LEFT JOIN** → 🟦 كل الصفوف من الجدول الأيسر + التطابق.
* **RIGHT JOIN** → 🟩 كل الصفوف من الجدول الأيمن + التطابق.
* **FULL OUTER JOIN** → 🟪 كل شيء + NULL إذا لم يتطابق.

**تخيل الجداول كشبكة أو أوراق Excel، وJoins كجسور تربط هذه الأوراق للحصول على المعلومات الكاملة.**

✅ **Takeaways:**

* تعلمنا إنشاء الجداول وإضافة البيانات وتحليلها باستخدام **Aggregate Functions وGROUP BY**.
* فهمنا كيفية استخدام **Joins المختلفة** لربط الجداول وتحليل البيانات.
* هذه القاعدة تصلح لأي نظام إدارة مبيعات صغير أو متوسط.
